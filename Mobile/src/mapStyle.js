// Map tiles/style for the in-app map (Search tab, location picker, venue mini-map).
// Uses MapLibre (open-source, no API key or billing account needed — unlike
// Google Maps) with OpenFreeMap's free hosted tiles (no key or account either).
export const MAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/positron';
